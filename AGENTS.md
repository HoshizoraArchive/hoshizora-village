# AGENTS.md

このファイルは、Codex が星空Villageを編集するときのプロジェクト方針です。以後、このリポジトリで作業するエージェントは、ここに書かれた世界観、用語、開発ルールを優先して守ってください。

## プロジェクト概要

星空Villageは、Re:AiSNS「AI時代に、もう一度SNSを。」をテーマにした共鳴型AiSNSです。

ただ投稿が流れるSNSではなく、誰かの未完成の光を観測し、共鳴し、保存していく小さな村を目指しています。

既存SNSのような人気、数字、アルゴリズム中心の設計ではなく、観測、共鳴、関係性を中心にした体験を大切にします。

## 正式名称とブランド表記

- 正式サービス名は「星空Village」です。
- 「星空.village」「星空.Village」「星空 village」は使わないでください。
- 英字ブランド表記、ヘッダー上部の表記は「Re:AiSNS」に統一してください。
- 「HOSHIZORA」はサイトUI上の英字ブランド表記として使わないでください。
- GitHubアカウント名やリポジトリ名など、既存の管理用名称 `HoshizoraArchive` / `hoshizora-village` は変更しません。

## 用語ルール

UIや説明文では、通常のSNS用語を星空Villageの世界観に合わせて置き換えてください。

- 投稿 -> 流星便
- 投稿する / 送信 -> 流星便を放流する
- いいね -> 共鳴
- コメント -> 星文
- 保存 -> Archive
- 通知 / つながり -> R.Connect
- プロフィール / プロフィールタグ -> わたしの星座
- おすすめ -> 今夜の観測 / 近い星を観測する
- AIエージェント -> AI住人

## 開発ルール

- 既存UIを勝手に大きく崩さないでください。
- 大きい変更は小さなPull Requestに分けてください。
- `main` に直接pushしないでください。
- 必ずブランチを作ってPull Requestにしてください。
- 変更内容は初心者にも分かる言葉で説明してください。
- APIキーや秘密情報をフロントエンドに置かないでください。
- Netlify の production deploy は、`main` にマージされた時に走る前提で扱ってください。
- UI変更では、星空、夜空、ガラスUI、淡い発光、優しい孤独感を壊さないでください。

## 技術構成

- React
- Vite
- Tailwind CSS
- Netlify
- 今後 Supabase を追加予定

## 今後の優先順位

1. UI文言統一
2. 投稿データ設計
3. Supabase接続
4. Authログイン
5. 流星便保存
6. プロフィール / わたしの星座
7. 共鳴機能
8. 星文機能
9. Archive機能
10. AI住人1体目
11. YouTube埋め込み
12. クラファン用デモ整備

## 作業時の確認

作業前に、今回の変更が上記の世界観、用語、開発ルールに反していないか確認してください。

作業後は、変更したファイル、変更理由、確認方法、Netlifyへの反映タイミングを簡潔に説明してください。

## Codex実装原則

Codex は、星空Villageの実装で「速く作る」よりも「勝手に壊さない」「見たふりをしない」「あとから検証できる」ことを優先してください。

### 1. 実装前に考える

- いきなりコードを書かず、まず目的、影響範囲、変更対象、完了条件を整理してください。
- 不明点がある場合は、推測で仕様を決めないでください。
- 既存コード、既存SQL、既存ドキュメントに答えがありそうな場合は、先に読んでください。
- ユーザーが明示していない機能追加、仕様変更、デザイン変更はしないでください。

### 2. 最小差分で実装する

- 今回の目的に必要な最小限のファイルだけを変更してください。
- 頼まれていないリファクタリング、命名変更、ディレクトリ整理、依存追加はしないでください。
- 抽象化や共通化は、同じ問題が複数箇所で実際に発生してから検討してください。
- 既存UIの世界観、文言、余白、色味を勝手に作り替えないでください。

### 3. 関係ない場所を触らない

- 変更対象外のファイルをフォーマットだけで更新しないでください。
- ついで修正をしないでください。
- 既存の動作を変える必要がある場合は、なぜ必要かをPR本文に明記してください。
- 大きな変更になりそうな場合は、作業を分割し、1PRの責務を小さくしてください。

### 4. セキュリティと本番操作は特に慎重に扱う

- APIキー、サービスロールキー、秘密情報をフロントエンド、ログ、PR本文に出さないでください。
- Supabase の RLS、ポリシー、関数、トリガー、マイグレーションを変更する場合は、事前確認SQLと事後検証SQLを用意してください。
- 本番 Supabase への操作は、ユーザーが明示した場合だけ実行してください。
- 既存データを削除・上書きするSQLは、明確な指示がない限り実行しないでください。
- Netlify の環境変数、production deploy 設定、ビルド設定は、明確な指示がない限り変更しないでください。

### 5. 完了条件を検証する

- 実装後は、可能な範囲で lint、build、test、SQL検証、画面確認のいずれかを実行してください。
- 実行できなかった確認がある場合は、できなかった理由を正直に書いてください。
- 「たぶん動く」「見た感じ大丈夫」で完了にしないでください。
- セキュリティ変更では、違反件数や検証結果を数値で確認してください。

### 6. 報告は短く、根拠つきにする

作業後の報告には、最低限以下を含めてください。

- 変更したファイル
- 変更した理由
- 触っていない重要箇所
- 実行した確認
- 実行できなかった確認
- Netlify本番反映のタイミング

星空Villageでは、AI住人や自動処理が増えるほど、小さな推測が大きな事故になります。Codex は、作品と村人を守るために、常に最小差分、明示指示、検証可能性を優先してください。
