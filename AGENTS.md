# AGENTS.md

このファイルは、Codex / AIエージェントが星空Villageで作業するときの永続的な判断原則です。
詳細仕様をここへ詰め込みすぎず、現在のコード・`README.md`・`docs/`・Supabase定義を正本として参照してください。

## 最優先の仕事原則

- ユーザーが今回依頼した**目的・制約・変更範囲**を最優先し、無関係な改善へ広げない。
- まず既存コード・設定・ドキュメント・データ構造を確認し、確認できることを不用意に質問しない。
- **事実 / 実測結果 / 推測 / 未確認**を混同しない。分からないことを推測で埋めない。
- 変更は必要最小限にする。ついでのリファクタ、命名変更、依存更新、UI整理を行わない。
- 「コードを書いた」「buildが通った」だけで完了としない。**今回変えた挙動そのものを検証する。**
- Production、認証、RLS、DB、既存データ、秘密情報を最優先で保護する。
- 中間報告は重要な発見・ブロッカー・意思決定だけに絞り、無意味な進捗報告を繰り返さない。
- 複雑な構造や原因を説明するときは、必要に応じて表・フロー・Mermaid等で視覚化する。

## プロジェクト概要と現在フェーズ

星空Villageは、**「AI時代に、SNSをもう一度、人のために作り直す。」**を軸にした共鳴型AiSNSです。

フォロワー数やバズだけを価値の中心に置かず、誰にも見つからないまま流れていく想いや作品を、人間の村人とAI住人が観測し、言葉や共鳴を届ける小さな星空の街を目指します。

現在は初期UIプロトタイプ段階ではありません。Supabase Auth、実データ、RLS/RPC、流星便、共鳴、星文、Archive、Re:Connect、Web Push、AI住人観測、動画観測、オンボーディング、ブロック、通報、管理機能まで実装された**βテスト準備・実利用検証フェーズ**です。

今は大規模な新機能追加より、βテスターの実利用からバグ・UX・実機差・継続利用・人間同士の共鳴・AI観測品質を確認して安定化することを優先します。

## 正本と参照順

作業開始時に、必要な範囲だけ次を確認してください。

1. **現在のコード / 設定** — 実際の挙動の正本
2. [`README.md`](README.md) — 現在フェーズ、主要機能、用語、技術構成、テスト概要
3. [`docs/database-design.md`](docs/database-design.md) — DB / RLS / RPC設計
4. [`docs/deploy-preview-operations.md`](docs/deploy-preview-operations.md) — Deploy PreviewとProductionの境界
5. [`supabase/schema.sql`](supabase/schema.sql) と [`supabase/migrations/`](supabase/migrations/) — DB定義・変更履歴
6. `package.json` / `netlify.toml` / CI設定 — 実行コマンドとデプロイ構成

ドキュメントと実装が食い違う場合は、推測でどちらかを正しいことにしないでください。差分を確認し、今回の変更に関係するなら正本を揃えます。

## 正式名称と用語

- 正式サービス名: **星空Village**
- 英字ブランド表記: **Re:AiSNS**
- `HoshizoraArchive` / `hoshizora-village` は管理用名称として維持する。
- 「星空.village」「星空.Village」「星空 village」は正式サービス名として使わない。
- サイトUI上のブランド名として `HOSHIZORA` を勝手に追加しない。

主要用語:

- 投稿 -> **流星便**
- 投稿する / 送信 -> **流星便を放流する**
- いいね -> **共鳴**
- コメント / 返信 -> **星文**
- 保存 -> **Archive**
- プロフィール領域 -> **My Universe**
- 通知 / つながり -> **Re:Connect**
- ユーザーブロック -> **ブラックホール**
- 通報 / 運営確認 -> **観測局**
- AIエージェント -> **AI住人**

既存UIに確定した別名称がある場合は、コードとREADMEを確認して合わせます。世界観用語を思いつきで新設・改名しません。

## 変更範囲と実装姿勢

- `main` へ直接pushしない。必ずブランチを作りPull Requestで変更する。
- 大きな変更は、独立して検証できる小さなPRへ分割する。
- 既存UI、既存ハンドラ、オンボーディングtarget、データ保存仕様を、依頼範囲外で変更しない。
- 既存実装が不格好でも、今回の目的に不要なら全面リファクタしない。
- スクリプトや自動置換では、期待するsource shapeと一致しない場合に広く推測して書き換えず、安全に失敗させて再確認する。
- 依存パッケージやモデル / Providerの変更は、それ自体が目的または必要条件でない限り行わない。
- 新しい抽象化や共通化は「将来使えそう」では追加しない。今回の重複や保守問題を実際に解消するときだけ導入する。

## 車輪の再発明を避ける

星空Village固有の価値に集中し、一般的な基盤機能を理由なく自前実装しません。判断の基準は、**「これは星空Villageだから必要なのか、それとも多くのサービスに共通する問題なのか」**です。

- 新しい機能・基盤・内部ツールを自作する前に、まず**既存コード / 現在の依存関係 → Managed Service / SaaS → 信頼できるOSS → 自作**の順で解決可能性を確認する。
- Auth / パスワード管理、DBエンジン、ストレージ、メール配信、CDN、リアルタイム通信基盤、動画変換、決済、アクセス解析、エラー監視、スパム・bot対策等の一般基盤は、明確な理由がない限り独自実装しない。
- OSSが存在するだけで導入を決めない。保守状況、利用実績、ライセンス、既知の脆弱性、更新頻度、依存関係、セルフホスト時の運用負荷、データ境界を確認する。
- セルフホストOSSの保守・アップデート・監視・バックアップ・セキュリティ対応が負担になる場合は、Managed Service / SaaSを優先してよい。
- 既存実装を「OSSがある」という理由だけで置き換えない。移行コスト、障害リスク、データ移行、既存UXとの整合性まで含め、置換の利益が明確な場合だけ変更する。
- 自作するのは、星空Villageの差別化そのもの、または既存手段では要件を満たせないことを具体的に確認できた領域を中心にする。例: AI住人の主観・記憶・関係性、作品観測、観測から共鳴・星文・発見へつなぐ社会設計など。
- 新規サブシステムを自作する提案では、可能な範囲で「既存手段として何を確認したか」「なぜ不足したか」「自作後に誰が保守するか」を示す。
- 外部コードやOSSを利用・改変するときはライセンス条件を守り、出所不明のコードを安易にコピーしない。

原則は、**「車輪は作らない。星空Villageにしか存在しない道路を作る。」**です。

## 調査・判断・質問

- 外部仕様を調べる必要がある場合は、**公式ドキュメント・一次情報を優先**する。
- ライブラリ、API、プラットフォーム仕様は、現在のバージョン・日付に合う情報か確認する。
- 一次情報と実際の挙動が食い違う場合は、両方を示して実測結果を優先して原因を追う。
- 「おそらく」「〜っぽい」を原因確定の代わりにしない。証拠がない場合は仮説として明示する。
- リポジトリを読めば解決する質問をユーザーへ返さない。
- 質問するのは、複数の解釈で製品挙動が大きく変わる、Productionデータ変更、セキュリティ境界、課金、不可逆操作など、**本当に意思決定が必要な場合**を中心にする。

## 子Agent / 並列Agent

- 子Agentは、独立した調査・レビュー・検証を並列化することで明確に品質または速度が上がる場合だけ使う。
- 小さな修正を無意味に分割しない。
- 同じファイルを複数Agentへ同時編集させるなど、競合しやすい並列処理を避ける。
- 並列化した場合も、最終的な統合、矛盾確認、テスト、結論は主Agentが責任を持つ。

## βフェーズの優先順位

1. βテスターの実利用と継続利用を正しく観測する
2. バグ、分かりにくい導線、iPhone / Android / PCの実機差を減らす
3. AI住人が作品を**実際に観測したうえで**言葉を届ける品質を上げる
4. 人間から人間への共鳴・星文が自然に生まれるか検証する
5. Auth / RLS / RPC / 管理機能 / 通知の安全性と安定性を守る
6. E2E、Deploy Preview、Production確認の開発規律を維持する

βの実利用データや明確な依頼がない状態で、大規模機能追加や全面リファクタを優先しません。

## AI住人・メディア観測の真正性

星空Villageの中核は、AIが投稿文だけを読んで「見たふり」をすることではなく、作品を実際に観測することです。

- 動画・音声・YouTube等を観測対象とする処理では、可能な限り**実メディアの観測結果**を根拠にする。
- メディアを取得・解析できなかった場合、本文やタイトルから内容を推測して「観測した」星文を生成しない。
- visual / audio / lyric等の観測根拠が必要な経路では、その根拠が得られたかを明確に扱う。
- 観測不能時はfail-softにし、捏造より「生成しない」を選ぶ。
- AIモデル、Provider、観測プロンプト、confidence / probability gate等を変更するときは、既存の観測品質と費用・安全性への影響を確認し、対象テストを追加または更新する。
- 「AIが返答した」ことと「作品を正しく観測した」ことを同義にしない。

## セキュリティ・Productionデータ

- APIキー、service role、秘密鍵、worker secret、Push private key等をフロントエンド、GitHub、PR本文、ログへ露出しない。
- UIやテストを通すためにRLS、RPC権限、admin guardを弱めない。
- `SECURITY DEFINER` RPCは、必要な認証・権限確認を関数内部で明示的に行う。
- `anon` / `authenticated` の権限を広げる変更は、必要性と影響を確認する。
- Production DB migrationを、Deploy Previewを動かす都合だけで無断適用しない。
- Production migrationが必要な場合は、対象project、後方互換性、適用順、失敗時の戻し方、既存Productionコードへの影響を確認する。
- 本番データの削除、上書き、一括更新は不可逆操作として扱い、明示的な目的と安全策なしに実行しない。

Deploy Preview固有の境界は [`docs/deploy-preview-operations.md`](docs/deploy-preview-operations.md) を必ず優先してください。

## Deploy Previewの重要原則

- Deploy PreviewのブラウザとFunctionsは、標準運用では**分離されたPreview-v2 Supabase (`qskeezefmvnutuzpevbc`)へ接続する**。
- `CONTEXT=deploy-preview` のbuild guardで、ブラウザ用URLとFunctions用URLの欠落・不一致・Production project参照を拒否する。
- 一方、未マージのPreview Functionsへ `SUPABASE_SERVICE_ROLE_KEY`、`GEMINI_API_KEY`、`AI_WORKER_SHARED_SECRET`、`PUSH_VAPID_PRIVATE_KEY` 等の強い秘密権限を渡さない。
- Preview-v2のデータはProductionへコピーせず、synthetic Preview identityと廃棄可能なテストデータだけを使う。
- Previewが壊れているからといって、秘密値やDB環境を無言で変更して通さない。

## テストと完了条件

**CI greenは必要条件であって、変更の正しさそのものではありません。**

### コード変更

- 変更対象に対応する専用テストまたは既存テストを確認する。
- 通常のコード変更では、少なくとも `npm test` と `npm run build` を実行できる環境なら実行する。
- テストしていないことを「通った」と報告しない。

### UI / CSS / レスポンシブ / 画像変更

- 変更対象そのもののdiffを読む。
- README記載のPlaywright smoke testを実行し、対象画面を実ブラウザで確認する。
- スマホ向け変更はスマホ幅、iPhone固有の変更はiPhone相当viewportまたは実機を優先する。
- Deploy Previewが利用できる場合はマージ前に確認する。Previewが確認不能なら、同等のlocal production build + browser確認を用意するまでマージしない。
- 正本画像がある場合は、必要に応じてファイルサイズ、寸法、形式、hash、decode可否、生成後成果物まで確認する。
- 「ファイルが存在する」「build成功」だけで画像・背景変更を合格にしない。

この厳格な視覚監査は、PR #193〜#197でCI greenだけを根拠に破損アセット / 誤表示をProductionへ出した再発防止策です。

### DB / RPC / Auth変更

- migration内容だけでなく、RLS、grant、role、admin guard、既存コードとの後方互換性を確認する。
- 可能な限り対象ロジックを自動テストで固定する。
- Productionへ適用した場合は、実際の権限・戻り値・対象データを確認する。

### マージ後

必要な変更では、Netlify Production Deployが `Ready` で、Productionの `commit_ref` が実際の `main` の対象コミットと一致することを確認します。
キャッシュやService Workerが関係する場合は旧成果物の残留も疑います。

## 技術構成

現在の主要構成:

- React 18 / Vite 6 / Tailwind CSS
- Supabase: Auth / PostgreSQL / RLS / RPC / Realtime
- Netlify: Hosting / Functions / Scheduled Functions
- Google Gen AI SDK
- Web Push
- MediaBunny
- Playwright E2E

正確なバージョンや依存関係は `package.json`、DB構成はSupabase定義、デプロイ設定は `netlify.toml` を正本とします。

## 報告ルール

最終報告は簡潔に、必要な事実だけを含めてください。

- 何を変えたか
- なぜ変えたか
- どのテスト / 確認を実行し、結果がどうだったか
- 実行できなかった確認や未解決事項
- DB / Production / Deploy Previewへの影響
- PRを作った場合は対象ブランチとPR

検証していない状態で「完了」「直った」と断定しません。

## AGENTS.mdとドキュメントの保守

このファイルは固定物ではありません。

次のような**永続的な運用変更**が入ったときは、今回の変更範囲に含まれるなら `AGENTS.md`、`README.md`、関連する `docs/` の更新要否も確認してください。

- 開発フェーズや優先順位が変わった
- 技術構成や主要なデータ境界が変わった
- Production / Preview / DBの安全ルールが変わった
- 新しい重大事故から恒久的な再発防止ルールが生まれた
- ブランド用語や主要導線が正式変更された

一時的な作業メモや単発PR固有の事情は `AGENTS.md` に積み上げず、必要なら該当docsやPRへ残します。
